# Copiar este archivo como .env y completar los valores.
# El .env no se versiona: cada integrante tiene el suyo.

DB_HOST=localhost
DB_PORT=5433
DB_NAME=bioequi_db
DB_USER=admin
DB_PASSWORD=password123
